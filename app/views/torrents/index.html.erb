<h1>Listing torrents</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Slug</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% collection.each do |torrent| %>
  <tr>
    <td><%= torrent.name %></td>
    <td><%= torrent.slug %></td>
    <td><%= link_to 'Show', torrent %></td>
    <td><%= link_to 'Edit', edit_torrent_path(torrent) if can? :manage, torrent %></td>
    <td><%= link_to 'Destroy', torrent, method: :delete, data: { confirm: 'Are you sure?' } if can? :manage, torrent %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Torrent', new_torrent_path if can? :create, Torrent %>
