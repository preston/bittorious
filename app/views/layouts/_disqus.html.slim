javascript:
    var disqus_shortname = 'bittorious';
    var disqus_identifier = '#{Rails.env}#{resource.id}';
    var disqus_url = '#{dashboard_url()}#!#{resource.slug}';
    var disqus_title = '#{resource.name}';
javascript:
    /* * * DON'T EDIT BELOW THIS LINE * * */
    if (typeof DISQUS == "undefined") { 
      (function() {
          var dsq = document.createElement('script'); dsq.type = 'text/javascript'; dsq.async = true;
          dsq.src = ('https:' == document.location.protocol ? 'https://' : 'http://') + disqus_shortname + '.disqus.com/embed.js';
          (document.getElementsByTagName('head')[0] || document.getElementsByTagName('body')[0]).appendChild(dsq);
      })();
      $('#disqus').fadeIn();
    } else {
      DISQUS.reset({
        reload: true,
        config: function () {  
          this.page.identifier = '#{Rails.env}#{resource.id}';  
          this.page.url = '#{dashboard_url()}#!#{resource.slug}';
          this.page.title = '#{resource.name}';
        }
      });
    }

noscript
    | Please enable JavaScript to view the
    a href="http://disqus.com/?ref_noscript"
        comments.
